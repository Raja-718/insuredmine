# Web app (bonus deployment layer). Install core libs first:
#   pip install -r requirements.txt
# then:
#   pip install -r web/requirements_web.txt
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
